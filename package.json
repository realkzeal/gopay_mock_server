{
  "name": "mock_server",
  "version": "1.0.0",
  "description": "Mock server for testing applications",
  "license": "ISC",
  "author": "Phillip Biobaku",
  "type": "module",
  "main": "index.js",
  "scripts": {  
    "build": "echo \"No build step needed for serverless functions\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "serverless-http": "^3.2.0"
  }
}